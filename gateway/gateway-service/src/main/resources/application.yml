server:
  port: 80
spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      enabled: true # ?????? ????
      routes:
        - id: login-service-route # ?????id ??????
          uri: http://localhost:8081 # uri ???????
          predicates:
            - Path=/doLogin # ???? ???Path????/doLogin ?? uri?? ????????uri??
      discovery:
        locator:
          enabled: true # ?????? ???????? ???????
          lower-case-service-id:  true # ????????
eureka:
  client:
    service-url:
      defaultZone: http://192.168.81.130:8761/eureka
    registry-fetch-interval-seconds: 3 # ?????????????
  instance:
    hostname: localhost
    instance-id: ${eureka.instance.hostname}:${spring.application.name}:${server.port}


